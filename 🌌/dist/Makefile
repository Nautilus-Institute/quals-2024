/target/🌌:
	cp -r /src /src🔥
	bash -c 'for file in /src🔥/*.✨; do mv "$$file" "$${file%.✨}.🔥"; done'
	cd /src🔥 && \
	 mojo build --debug-level 'line-tables' -O0 \
	 	-o /target/🌌 \
		./main.🔥